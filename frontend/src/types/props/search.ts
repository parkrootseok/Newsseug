export interface KeywordProps {
  isHistory: boolean;
  keywordText: string;
}

export interface InputSectionProps {
  keywordText?: string;
}

export interface KeywordItem {
  keywordText: string;
  isHistory: boolean;
}
